# 가상환경 (requirements.txt 로 각자 다시 만듭니다)
.venv/

# 파이썬
__pycache__/
*.py[cod]

# 데이터베이스 — sync_tarkov 로 언제든 다시 채울 수 있으므로 올리지 않습니다
db.sqlite3

# API ETag 캐시 (각자 로컬 상태)
.sync_etag.json

# 에디터 / OS
.vscode/
.idea/
Thumbs.db
desktop.ini
